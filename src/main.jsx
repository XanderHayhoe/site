import "./index.css"; // must be first
import React from "react";
import ReactDOM from "react-dom/client";
import XanderPortfolio from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <XanderPortfolio />
  </React.StrictMode>
);
